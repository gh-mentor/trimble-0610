# Create a shell script that uses git to stage, commit, and push all changes to the 'main' branch remote repository. 

# Stage all changes
git add .

# Commit all changes

git commit -m "Update"

# Push all changes to the 'main' branch remote repository
git push origin main